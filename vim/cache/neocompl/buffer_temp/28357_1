
import System.Random(randomRIO)
i

shuffle xs = do
  takes <- mapM (randomRIO . (,) 1) [1..length xs]
  return . map snd . sortBy (comparing fst) $ zip takes xs

main = do
  rlist <- shuffle [1..1000]
  print rlist
